package com.engagementMusic.engagementMusic.Repositories;

import com.engagementMusic.engagementMusic.Models.Agent;
import com.engagementMusic.engagementMusic.Models.DancingBar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DancingBarRepository extends JpaRepository<DancingBar, Long> {
}
